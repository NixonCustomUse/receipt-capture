# AGENTS.md

This repository is **empty** — no commits, no source files, no configuration.

## Current state

- A bare `.git` directory exists at the root.
- `README.md` and `AGENTS.md` exist but are untracked.
- No `package.json`, manifests, or any source files are present.

## Working here

- Initialize the project from scratch as needed (e.g., `npm init`, `git init` has already been run).
- No build, test, lint, or formatting commands exist yet.
- No framework, language runtime, or toolchain conventions are established.
- There are no existing instruction files (`CLAUDE.md`, `.cursorrules`, `opencode.json`, etc.).

Establish tooling and conventions before writing significant code. Pin runtime versions and commit config first so later agents can rely on them.
